build_file: "gfxstream/.kokoro/presubmit.sh"

action {
  define_artifacts {
    regex: "*/*sponge_log.xml" # Actually a glob, not a regex
    regex: "*/*sponge_log.log"
    regex: "*/kokoro_test_outputs/**" # match all files regardless of directory depth
  }
}
